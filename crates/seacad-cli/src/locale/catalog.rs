//! Compile-time human-interface catalog shared by every CLI locale.

#[derive(Clone, Copy, Debug)]
pub(crate) struct CliCatalog {
    pub(crate) help_template: &'static str,
    pub(crate) internal_cli_error: &'static str,
    pub(crate) failed_to_write_output: &'static str,
    pub(crate) options_heading: &'static str,
    pub(crate) application_about: &'static str,
    pub(crate) root_usage: &'static str,
    pub(crate) commands_heading: &'static str,
    pub(crate) command_value_name: &'static str,
    pub(crate) emit_json_help: &'static str,
    pub(crate) large_profile_help: &'static str,
    pub(crate) show_path_help: &'static str,
    pub(crate) language_value_name: &'static str,
    pub(crate) language_help: &'static str,
    pub(crate) inspect_about: &'static str,
    pub(crate) verify_about: &'static str,
    pub(crate) inspect_usage: &'static str,
    pub(crate) verify_usage: &'static str,
    pub(crate) generic_file_usage: &'static str,
    pub(crate) arguments_heading: &'static str,
    pub(crate) file_value_name: &'static str,
    pub(crate) file_help: &'static str,
    pub(crate) mode_value_name: &'static str,
    pub(crate) mode_help: &'static str,
    pub(crate) print_help: &'static str,
    pub(crate) print_version: &'static str,
    pub(crate) status_label: &'static str,
    pub(crate) read_mode_label: &'static str,
    pub(crate) resource_profile_label: &'static str,
    pub(crate) physical_format_label: &'static str,
    pub(crate) path_label: &'static str,
    pub(crate) bytes_label: &'static str,
    pub(crate) source_id_label: &'static str,
    pub(crate) conformance_label: &'static str,
    pub(crate) groups_label: &'static str,
    pub(crate) absent: &'static str,
    pub(crate) eof_occurrence_label: &'static str,
    pub(crate) trailing_bytes_label: &'static str,
    pub(crate) diagnostics_label: &'static str,
    pub(crate) error_label: &'static str,
}

impl CliCatalog {
    #[cfg(test)]
    pub(crate) fn entries(self) -> [&'static str; 39] {
        [
            self.help_template,
            self.internal_cli_error,
            self.failed_to_write_output,
            self.options_heading,
            self.application_about,
            self.root_usage,
            self.commands_heading,
            self.command_value_name,
            self.emit_json_help,
            self.large_profile_help,
            self.show_path_help,
            self.language_value_name,
            self.language_help,
            self.inspect_about,
            self.verify_about,
            self.inspect_usage,
            self.verify_usage,
            self.generic_file_usage,
            self.arguments_heading,
            self.file_value_name,
            self.file_help,
            self.mode_value_name,
            self.mode_help,
            self.print_help,
            self.print_version,
            self.status_label,
            self.read_mode_label,
            self.resource_profile_label,
            self.physical_format_label,
            self.path_label,
            self.bytes_label,
            self.source_id_label,
            self.conformance_label,
            self.groups_label,
            self.absent,
            self.eof_occurrence_label,
            self.trailing_bytes_label,
            self.diagnostics_label,
            self.error_label,
        ]
    }
}
